### Compile
there is no need to compile on windows but on linux you must first have nlohmann json after navigate to the Builder directory and run  
`<my_cpp_compiler> CBuild.cpp -o CBuild`  
after navigate back to the original directory and run  
`CBuild`  
now you should have zedc set up!
